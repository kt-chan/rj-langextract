{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python Debugger",
            "type": "debugpy",
            "request": "launch",
            "program": "scripts/create_provider_plugin.py",
            "args": ["glmProvider", "--with-schema", "--test-only"],
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal"
        }
    ]
}